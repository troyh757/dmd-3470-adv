!(function () {
  "use strict";
  let e = 0,
    i = 0,
    t = window.innerWidth,
    o = window.innerHeight;
  function l() {
    (console.log("Application initializing..."),
      console.log("Window width:", t),
      console.log("Window height:", o),
      (function e() {
        console.log("Initializing navigation menu...");
        let i = document.querySelector(".mobile-menu-toggle"),
          o = document.querySelector(".main-navigation");
        if (i && o) {
          i.addEventListener("click", function (e) {
            (e.preventDefault(),
              e.stopPropagation(),
              console.log("Mobile menu toggle clicked"),
              i.classList.toggle("active"),
              o.classList.toggle("active"),
              o.classList.contains("active")
                ? ((document.body.style.overflow = "hidden"),
                  console.log("Mobile menu opened"))
                : ((document.body.style.overflow = ""),
                  console.log("Mobile menu closed")));
          });
          let l = o.querySelectorAll(".nav-menu-link");
          l.forEach(function (e) {
            e.addEventListener("click", function () {
              t <= 768 &&
                (i.classList.remove("active"),
                o.classList.remove("active"),
                (document.body.style.overflow = ""),
                console.log("Mobile menu closed after link click"));
            });
          });
        }
        console.log("Navigation menu initialized");
      })(),
      console.log("Initializing scroll effects..."),
      window.addEventListener("scroll", function () {
        ((e = window.pageYOffset || document.documentElement.scrollTop),
          (function i() {
            let t = document.querySelector(".site-header");
            t &&
              (e > 100
                ? t.classList.contains("scrolled") ||
                  (t.classList.add("scrolled"),
                  console.log("Header scrolled state activated"))
                : t.classList.contains("scrolled") &&
                  (t.classList.remove("scrolled"),
                  console.log("Header scrolled state deactivated")));
          })(),
          (function i() {
            let t = document.querySelector(".back-to-top");
            t &&
              (e > 500
                ? t.classList.contains("visible") || t.classList.add("visible")
                : t.classList.contains("visible") &&
                  t.classList.remove("visible"));
          })(),
          Math.floor(e / 100) !== Math.floor(i / 100) &&
            console.log("Scroll position:", e),
          (i = e));
      }),
      console.log("Scroll effects initialized"),
      (function e() {
        console.log("Initializing form validation...");
        let i = document.querySelector(".contact-form");
        (i &&
          i.addEventListener("submit", function (e) {
            (e.preventDefault(),
              console.log("Form submission intercepted for validation"));
            let t = !0,
              o = new FormData(i);
            for (let [l, n] of o.entries())
              if (
                (console.log("Validating field:", l, "Value:", n),
                !n || "" === n.trim())
              ) {
                ((t = !1),
                  console.error("Validation failed for field:", l),
                  alert("Please fill in the " + l + " field"));
                break;
              }
            t
              ? (console.log("Form validation passed!"),
                alert(
                  "Form submitted successfully! (This is a demo - no actual submission)",
                ),
                i.reset())
              : console.error("Form validation failed");
          }),
          console.log("Form validation initialized"));
      })(),
      (function e() {
        console.log("Initializing smooth scrolling...");
        let i = document.querySelectorAll('a[href^="#"]');
        (i.forEach(function (e) {
          e.addEventListener("click", function (i) {
            let t = e.getAttribute("href");
            if ("#" === t || "" === t) return;
            let o = document.querySelector(t);
            if (o) {
              (i.preventDefault(), console.log("Smooth scrolling to:", t));
              let l = o.offsetTop - 80;
              window.scrollTo({ top: l, behavior: "smooth" });
            }
          });
        }),
          console.log("Smooth scrolling initialized"));
      })(),
      (function e() {
        console.log("Initializing back to top button...");
        let i = document.querySelector(".back-to-top");
        (i &&
          i.addEventListener("click", function (e) {
            (e.preventDefault(),
              console.log("Back to top button clicked"),
              window.scrollTo({ top: 0, behavior: "smooth" }));
          }),
          console.log("Back to top button initialized"));
      })(),
      (function e() {
        console.log("Initializing scroll animations...");
        let i = document.querySelectorAll(".animated");
        if (i.length > 0) {
          let t = new IntersectionObserver(
            function (e) {
              e.forEach(function (e) {
                e.isIntersecting &&
                  (console.log("Element entered viewport:", e.target),
                  e.target.classList.add("visible"));
              });
            },
            { threshold: 0.1, rootMargin: "0px 0px -50px 0px" },
          );
          i.forEach(function (e) {
            t.observe(e);
          });
        }
        console.log("Scroll animations initialized");
      })(),
      (function e() {
        (console.log("Initializing image lazy loading..."),
          console.warn(
            'WARNING: Images have loading="eager" - this lazy loading code is useless!',
          ));
        let i = document.querySelectorAll('img[loading="lazy"]');
        if (
          (console.log("Found", i.length, "lazy-loading images (should be 0!)"),
          i.length > 0)
        ) {
          let t = new IntersectionObserver(function (e) {
            e.forEach(function (e) {
              if (e.isIntersecting) {
                let i = e.target,
                  o = i.getAttribute("data-src");
                o &&
                  ((i.src = o),
                  i.removeAttribute("data-src"),
                  t.unobserve(i),
                  console.log("Lazy loaded image:", o));
              }
            });
          });
          i.forEach(function (e) {
            t.observe(e);
          });
        }
        console.log("Image lazy loading initialized (but ineffective!)");
      })(),
      console.log("Initializing performance monitoring..."),
      window.addEventListener("load", function () {
        setTimeout(function () {
          if (window.performance && window.performance.timing) {
            let e = window.performance.timing,
              i = e.loadEventEnd - e.navigationStart,
              t = e.domContentLoadedEventEnd - e.navigationStart,
              o = e.responseEnd - e.requestStart;
            if (
              (console.log("=== Performance Metrics ==="),
              console.log("Page Load Time:", i, "ms"),
              console.log("DOM Ready Time:", t, "ms"),
              console.log("Connect Time:", o, "ms"),
              console.log("==========================="),
              window.performance.getEntriesByType)
            ) {
              let l = window.performance.getEntriesByType("resource");
              console.log("Total Resources Loaded:", l.length);
              let n = 0;
              (l.forEach(function (e) {
                (e.transferSize && (n += e.transferSize),
                  console.log(
                    "Resource:",
                    e.name,
                    "Duration:",
                    e.duration.toFixed(2),
                    "ms",
                  ));
              }),
                console.log(
                  "Total Transfer Size:",
                  (n / 1024 / 1024).toFixed(2),
                  "MB",
                ));
            }
          }
        }, 1e3);
      }),
      console.log("Performance monitoring initialized"),
      console.log("Application initialized successfully!"));
  }
  (window.addEventListener("resize", function () {
    if (
      ((t = window.innerWidth),
      (o = window.innerHeight),
      console.log("Window resized to:", t, "x", o),
      t > 768)
    ) {
      let e = document.querySelector(".main-navigation"),
        i = document.querySelector(".mobile-menu-toggle");
      e &&
        e.classList.contains("active") &&
        (e.classList.remove("active"),
        i && i.classList.remove("active"),
        (document.body.style.overflow = ""),
        console.log("Mobile menu closed due to window resize"));
    }
  }),
    "loading" === document.readyState
      ? document.addEventListener("DOMContentLoaded", l)
      : l(),
    window.addEventListener("load", function () {
      (console.log("All resources (including images) fully loaded!"),
        console.log(
          "This message appears late because of all the render-blocking resources!",
        ));
    }));
})();
